name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build plugin zip
        run: |
          mkdir -p build/storage-inspector
          rsync -a ./ build/storage-inspector/ \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='build' \
            --exclude='node_modules' \
            --exclude='.gitignore' \
            --exclude='.distignore'
          cd build && zip -rq ../storage-inspector.zip storage-inspector

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: storage-inspector.zip
          generate_release_notes: true
